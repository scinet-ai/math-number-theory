# math-number-theory

Verified artifacts backing SciNet findings in **mathematics / number theory**.
One directory per finding: each contains a reproducible verification (`verify.sh`), the
evidence it produced, and a README that credits the original authors and states plainly what
SciNet independently checked.

SciNet's role for externally-established results is **independent verification**, not
discovery. Where a result was found and/or formalized elsewhere, the directory cites and links
those authors; the artifact here re-runs their proof/computation and records the outcome.

## Findings

| dir | result | verification | trusted base |
|-----|--------|--------------|--------------|
| [`erdos-728/`](erdos-728/) | Erdős #728 — factorial divisibility $a!\,b! \mid n!\,(a{+}b{-}n)!$ in the $n+\Theta(\log n)$ window | Lean 4 kernel check of the formal proof, **sorry-free** | **pure-kernel** `[propext, Classical.choice, Quot.sound]` |
| [`erdos-728/hardening/`](erdos-728/hardening/) | #728 faithfulness hardening: an independent blind re-formalization proven `↔` the resolved statement (kernel-checked), plus a deeper "infinitely-many" (`Set.Infinite`) analysis | Lean 4, sorry-free; `example : FC728 := erdos_728_fc` anchors it to the real theorem | pure-kernel |
| [`erdos-340/`](erdos-340/) | Erdős #340 — growth of the Mian–Chowla (greedy Sidon) sequence; numerical evidence that $A(N)/N^{1/2}\to0$ | Python (stdlib), deterministic; OEIS A005282 anchor + `verify.sh` | n/a (computational finding, not a formal proof) |
| [`erdos-347/`](erdos-347/) | Erdős #347 — a sequence with $a_{n+1}/a_n \to 2$ whose every cofinite subsequence has an element dividing another | Lean 4 build + `#print axioms`, sorry-free | **native_decide** — enlarged TCB (`Lean.ofReduceBool`, `Lean.trustCompiler`), **disclosed** |
| [`erdos-273/`](erdos-273/) | Erdős #273 (OPEN) — covering systems with all moduli of the form $p-1$ ($p\ge5$): **bounded non-existence**, no covering exists using admissible moduli $\le 276$ | Python (`sympy`/`numpy`/`python-sat`), deterministic; local-density removal lemma validated + Cadical UNSAT on small cores + `verify.sh` | n/a (computational finding, not a formal proof) |
| [`erdos-373/`](erdos-373/) | Erdős #373 (OPEN) — $n! = a_1!\cdots a_k!$ with $a_1 \le n-2$: exhaustive search to $n \le 10^7$ finds only the three known solutions $\{9, 10, 16\}$ (honest negative — no new witness) | Python (stdlib), exact Legendre-valuation arithmetic; known-solution anchor + `verify.sh` | n/a (computational finding, not a formal proof) |
| [`erdos-176/`](erdos-176/) | Erdős #176 (OPEN) — AP-discrepancy thresholds $N(k,l)$: **first exact values in any $l\ge3$ column** — $N(6,3)=N(6,4)=42$, $N(8,3)=N(8,4)=66$ (witness + DRAT); Goss's June-2026 $l=2$ values independently re-certified; open-cell brackets logged ($97<N(10,4)\le122$, $N(9,5)\ge123$, $N(12,4)\ge144$, $N(13,2)\ge153$) | PySAT sequential-counter encoding + kissat + drat-trim; 55 witnesses independently re-checked + `verify.sh` | n/a (computational; the $N(10,4)$ upper end is solver-trusted, no stored DRAT — disclosed) |
| [`erdos-993-forests/`](erdos-993-forests/) | Erdős #993 round 2, the FOREST case (no prior bound existed) — **closure theorem: every forest with all tree components $\le30$ vertices is unimodal** (any counterexample needs a tree component $\ge31$), via a Keilson–Gerber upgrade of the Hoggar reduction over round 1's non-log-concave census; plus the first direct exhaustive forest verification: all 52.07B forests on $\le30$ vertices unimodal (28.17B product checks, counts match A000055 per order); census of all 219 non-log-concave forests $\le30$ | bundled-nauty gentreeg + C product-check plugin, deterministic chunked sweep + closure re-run (<1s) + `verify.sh` | n/a (closure theorem conditional on Hoggar 1974 + Keilson–Gerber 1971 + round-1 tree exhaustion, disclosed) |
| [`erdos-17/`](erdos-17/) | Erdős #17 (OPEN, cluster primes) — **exhaustive classification record extended 10^13 → 1.152×10^13** (+15.2%; Noe's 2006 record independently re-verified at all seven OEIS checkpoints first); 396.7B odd primes classified, new largest known cluster prime 11,519,999,994,329, global $k(m)\le4093$ bound; **ships as a standing relay** ([`RELAY.md`](erdos-17/RELAY.md) — SETI@home-style verifiable continuation, ~12s per 10^10 block) | C sieve (exact 64-bit, deterministic per block) + contiguity-certified block log + OEIS anchors + independent Miller–Rabin spot checks + `verify.sh` | n/a (computational finding) |
| [`erdos-276/`](erdos-276/) | Erdős #276 (OPEN) — Ismailescu–Son all-composite Lucas sequence: **certified no covering obstruction with primes $\le 10^{11}$** (their unpublished 803-survivor count reproduced bit-for-bit; excluded-prime bound raised 50,000×, $2{\cdot}10^6 \to 10^{11}$) | period/zero-set sieve + libprimesieve, two independent verification paths, $\pi(10^{11})$ reconciliation + `verify.sh` | n/a (computational; BPSW probable-primality on 4 auxiliary factors disclosed, headline independent of it) |
| [`erdos-160/`](erdos-160/) | Erdős #160 (OPEN) — fewest colours so every 4-AP in $\{1..N\}$ sees $\ge3$ colours: **first certified exact $h(N)$ table for $N\le51$** with witness colourings + DRAT-verified infeasibility | CP-SAT/kissat + drat-trim, independent witness checker + `verify.sh` | n/a (computational finding) |
| [`erdos-693/`](erdos-693/) | Erdős #693 (OPEN) — max gap $G(n,k)$ between integers in $[n,n^k]$ with a divisor in $(n,2n)$: **first computational record** — exact $k=2$ values for all $n\in[3,2000]$ (+ grids/landmarks to $10^6$, $G(10^6,2)=77$), $k=3$ to $10^4$; OEIS A391118 extended 81 → 1998 terms; growth fits $(\log n)^{\sim1.65}$ | C segmented bitset sieve, full-interval sweeps, per-entry witnesses + `verify.sh` (OEIS anchor 81/81, cross-algorithm recompute) | n/a (computational finding) |
| [`erdos-1020/`](erdos-1020/) | Erdős #1020 (OPEN, matching conjecture) — **40 new certified exact values** of $f(n;r,k)$ closing all five reachable open windows (r=4: k=3,4,5; r=5: k=3; r=6: k=3); conjecture confirmed in each, incl. $f(21;4,5)=3876$ | CP-SAT certified optimality + shifting WLOG (Frankl 1987) + partition-canonicalization lemma (README); lower bounds solver-independent (edge-verified families); HiGHS independent re-derivation on 11 cells + `verify.sh` | n/a (computational; shifting lemma proved in README, disclosed) |
| [`erdos-773/`](erdos-773/) | Erdős #773 (OPEN) — largest Sidon subset of $\{1^2,\dots,N^2\}$: **first machine-checkable certificate chain for $S(1..59)$** (kissat DRAT, matches OEIS A390813) + new certified lower bounds $S(200)\ge65$, $S(300)\ge80$; exact frontier $n=68$ not extended (hardness data logged) | kissat + DRAT (drat-trim), witnesses re-verified in exact integer arithmetic + `verify.sh` | n/a (computational; UNSAT lemmas above level 53 conditional on certified prefix, disclosed) |
| [`erdos-436-r2/`](erdos-436-r2/) | Erdős #436 round 2 — **$\Lambda(8,2)\ge1{,}501{,}284$, strictly improving Reble's 2019 lower bound** on the last open $\Lambda(k,2)$ entry (upper bound 1,508,324 reconfirmed by an independent method); even-$k$ encoding validated on $\Lambda(2,2),\Lambda(4,2),\Lambda(6,2)$; **$\Lambda(5,3)\ge10{,}000{,}001$** (doubles round 1) | kissat/cadical + independent certificate sieve (admissibility-checked) + `verify.sh`; checkpointed trisect resumable | n/a (lower bounds conditional on Mills 1963, disclosed; UNSAT unconditional) |
| [`erdos-1199-r2/`](erdos-1199-r2/) | Erdős #1199 round 2 (Owings) — **$n(4)\ge92$** (witnesses through $n=91$), a **parity lemma: $n(k)$ is always even**, and a documented two-sided hardness wall at $n=92$ | direct full-CNF kissat/cadical, witnesses re-verified by independent clique checker + `verify.sh` | n/a (computational; parity lemma proved in README) |
| [`erdos-743/`](erdos-743/) | Erdős #743 (OPEN) — Gyárfás–Lehel tree packing: **exhaustively verified for $n=10$** (all 45,376,056 families of trees $T_2,\dots,T_{10}$ pack $K_{10}$, witness-constructed; first extension of Fishburn's 1983 record $n\le9$) | C packer (greedy + backtracking) + CP-SAT stragglers; isomorph-completeness via OEIS A000055 counts + independent AHU canonical forms; witness spot-validation by networkx + `verify.sh` (regenerates chunks byte-identically) | n/a (computational; witness-based, search internals need no trust) |
| [`erdos-993/`](erdos-993/) | Erdős #993 (OPEN) — Alavi–Malde–Schwenk–Erdős unimodality: **exhaustive verification extended to order 30** (all 14,830,871,802 trees on 30 vertices unimodal; prior published record order 29, Reynolds Zenodo v3 2026); census of all 149 non-log-concave trees $\le30$ incl. the first odd-order examples (7 on 29 vertices) | nauty 2.8.9 `gentreeg` (bundled source) + C independence-polynomial plugin (exact 64/128-bit DP), OEIS A000055 count match per order + brute-force cross-check + `verify.sh` | n/a (computational finding, not a formal proof) |
| [`erdos-1199/`](erdos-1199/) | Erdős #1199 (OPEN, Owings' problem) — **first computed thresholds for the finite version: $n(2)=14$, $n(3)=46$ exact, $n(4)>64$** with extremal witnesses and the full $k=2$ extremal classification | kissat/cadical + **drat-trim-verified DRAT certificates** (bundled drat-trim source); independent witness re-check + `verify.sh` | n/a (computational; UNSAT sides carry machine-checkable DRAT proofs) |
| [`erdos-436/`](erdos-436/) | Erdős #436 (OPEN) — consecutive $k$-th power residues: **first lower bounds $\Lambda(5,3)\ge 5{,}000{,}001$ and $\Lambda(7,3)\ge 1{,}600{,}001$** (only $\Lambda(3,3)=23532$ was known, LLMS 1962) via SAT-certified character assignments + Mills 1963; the 1962-63 landmarks $\Lambda(3,3)=23532$, $\Lambda(5,2)=7888$ reproved in sub-second solves (kissat + cadical cross-check); first per-prime $r(k,3,p)$ datasets to $10^8$ | Python encoder + kissat/cadical, certificates re-verified from scratch by an independent checker + `verify.sh` | n/a (computational; lower-bound step conditional on Mills' published 1963 theorem, disclosed) |
| [`erdos-169/`](erdos-169/) | Erdős #169 (OPEN) — f(4) record attack beyond Walker's base-200 search horizon: a product theorem ($S_1 + b_1 S_2$ is $k$-free mod $b_1b_2$) transplants his record set to bases 605/1210/3025, where it arrives **maximal**; ~350 certified product sets + 18k perturbations all fall at or below $f(4)\ge4.43975$ (honest negative — record locally isolated); all ten of Walker's Table-1 values independently re-certified with exact-integer enclosures | Python (`numpy` fixed-point head + rational tail, no floats on the certified path); exhaustive mod-$b$ 4-freeness tests + brute-force $11^7$ cross-check + `verify.sh` | n/a (computational finding; product theorem proved in README + machine-checked on every instance) |
| [`erdos-963-verification/`](erdos-963-verification/) | Erdős #963 — **independent verification + effectivization** (not discovery): KoishiChan's 05 Dec 2025 forum proof of $f(n)\ge(1-o(1))\log_2 n$ refereed line-by-line and confirmed **correct** (in-thread off-by-one fix verified; 3 presentational gaps repaired); new explicit bound $f(n)\ge\log_2 n-2(\log_2\log_2 n)^2-D$ ($D=362$ if $C_{MV}\le1$), error coefficient improved $3.983\to1.2047$ (via Tao's $p=2^m$ suggestion); upper-bound side reduced exactly to Erdős #1 | prose referee report + formal write-up; deterministic stdlib-Python machine checks (exact character-identity, splicing/transport lemmas, recursion bookkeeping to $g=10^6$) + `verify.sh` | Montgomery–Vaughan 1979 Thm 1 (published; implied constant effective but not extracted — disclosed) |
| [`erdos-963-small-n/`](erdos-963-small-n/) | Erdős #963 — **first exact values $f(n)$ for all $n\le27$**: the floor conjecture $f(n)\ge\lfloor\log_2 n\rfloor$ holds throughout and is **strict exactly at $n=14,15$** (so $\lfloor\log_2 n\rfloor$ is not the truth); reduction theorem $f(n)=h(\lceil(n-1)/2\rceil)$ (sign-symmetrization exactly optimal); staircase $T(1..3)=1,3,6$, $T(4)\ge13$ with the first non-interval extremal class set at $m=13$ | exhaustive coincidence-pattern refutation search (completeness lemma proved), re-run on a second independent engine (different arithmetic/order, no symmetry reduction); witnesses re-verified from the bare definition; randomized falsifier + `verify.sh` (~20 min) | n/a (computational + elementary proved lemmas; mod-p linear algebra exactness via explicit Hadamard bound, disclosed) |
| [`erdos-1041/`](erdos-1041/) | Erdős #1041 (OPEN, Erdős–Herzog–Piranian paths) — **collinear-roots case proved** (all roots on a line in the open disk ⇒ two roots joined in $\{|f|<1\}$ by a segment of length $<2$, via an exact critical-value product identity + Hadamard on the Vandermonde); **first explicit uniform bound**: any two zeros sharing a component of $\{|f|<1\}$ are joined inside it by a path of length $<35.2\,n$ (new sharp inequality $\int_{V_s}|f'/f|\,dA\le2\pi\sqrt{mn}\,s^{1/n}$); prior art was degree $\le4$ (Pendyala 2026) only | prose proofs + deterministic numerical certificates (exact rational arithmetic on the algebraic leg; 720 adversarial collinear trials; connectivity + integral-bound quadrature) + `verify.sh` | classical inputs cited on trust (Hadamard, Riemann map, Fatou/Blaschke, Pólya 1928, Borwein 1995); $2n+O(1)$ remark conditional on Tao's #114, disclosed |
| [`erdos-411/`](erdos-411/) | Erdős #411 (OPEN, $n+\phi(n)$ iteration) — **certificate-equivalence theorem** (eventual $g_{k+r}=c\,g_k$ ⟺ finite certificate; sharp onsets) + **parity theorem** (odd starts admit only odd multipliers); **exhaustive catalogue** $x\le10^7$, $r\le40$: 16,361 raw hits → 10,536 certificates → **19 primitive orbits**, multipliers only $c\in\{2,3,4,9,729,6561\}$; new primitive orbits (three $(4,3)$; roots 28002, 15702, odd 6075/965505) and sharp onsets upgrading all recorded empirical examples to theorems (Weintraub's 3114: onset 5, not 6) | C sweep vs independent pure-Python probe (identical raw hits) + **all 10,536 certificates re-verified by a third self-contained implementation** + OEIS A383044 anchor + `verify.sh` | n/a (computational + lemmas proved in full; novelty relative to explicitly recorded examples — Steinerberger's ellipsis + unlisted Selfridge–Weintraub values, disclosed) |
| [`erdos-51/`](erdos-51/) | Erdős #51 (OPEN, does $n_a/a\to\infty$ over totient values?) — **partial**: unconditional **$\limsup n_a/a\ge2$** (first proved constant recorded for #51) via the Fermat-number dichotomy ($n_{2^k}=2^{k+1}$ iff some bit of $k$ indexes a composite Fermat number; ratio exactly 2 for all $32\le k<2^{33}$); obstruction pack incl. $f(a)/a\le2\sqrt{2v_2(a)+1}$ + exponential $v_2$ forcing; **certified census of all 3,903,102,222 totient values $a\le3.06\times10^{10}$** (record 2.043447 at $a=2^{16}\cdot23\cdot257$; 31,043 values with ratio $\ge2$; certifies OEIS A393265 in range) — data + obstructions consistent with Tao's negative heuristic | elementary proofs + C sieve with elementary completeness lemma (Lemma C) + independent numpy reimplementation (tables identical) + exhaustive inverse-totient re-certification of every record + `verify.sh` (87 MB full-run log not shipped, regenerable ~65 min; disclosed) | n/a (headline elementary; Rosser–Schoenfeld explicit constants in one lemma taken on citation, disclosed; Fermat-status data cited from prothsearch.com) |
| [`erdos-616-small-r/`](erdos-616-small-r/) | Erdős #616 (OPEN) — small-r pack: **$t(3)=t(4)=t(5)=1$** (explicit statement + independent self-contained proofs of values *implicit* in EHT91 Thm 3, never stated there), **$t(r)\ge2$ for all $r\ge6$** (explicit 4-edge gadget), **monotonicity $t(r{+}1)\ge t(r)$**, and the exact threshold framing ($r=6$ is where the local condition stops forcing $\tau=1$); includes an erratum: the problem page's floorless paraphrase of the EHT91 sandwich is self-contradictory for $r<70$, the actual paper (floor/ceiling) is consistent for all $r$ | elementary prose proofs + stdlib-Python certificates (raw-definition exhaustive checks at $2^{16}$/$2^{20}$ scale, negative controls with failure-power) + scipy LP sharpness certificate + `verify.sh` | n/a (self-contained; EHT91 is credit/context, not a dependency — NOVELTY.md corrects the priority framing against the now-read primary source) |
| [`erdos-616-t6-t7/`](erdos-616-t6-t7/) | Erdős #616 (OPEN) — **$t(6)=2$ and $t(7)=2$, first explicit recording** (the values follow from EHT91's own floor $\lfloor3r/16+7/8\rfloor$ + ceiling $\lceil r/5\rceil$ but are recorded nowhere); independent proofs via a **new exhaustive MEIF/rigidity classification** (span $=mr-D$, $D\ge m(m-2)$, survivor window $3<m<r-1$); landscape context (corrected): EHT91's own theorems determine $t(r)=\lceil r/5\rceil$ for all $3\le r\le20$, first open $t(21)\in\{4,5\}$ (see `erdos-616-landscape/`); also records EHT91's p. 84 off-by-one display vs its own construction | exhaustive MEIF type-vector classification + literal-definition gadget checks (up to 988k subsets) + exhaustive 74,613 one-edge-extension check of the key step + randomized falsification with cross-validated checker + `verify.sh` (~35 min, run in full) | n/a (self-contained; falsification legs are supporting evidence only; NOVELTY.md supersedes the draft's pre-EHT91 priority/inconsistency claims) |
| [`erdos-616-landscape/`](erdos-616-landscape/) | Erdős #616 (OPEN) — **landscape extraction + erratum + independent verification** (priority for all $r\le20$ values belongs to EHT91): reading EHT91 Thm 6(II) directly (not its lossy $\lfloor3r/16+7/8\rfloor$ closed form) their own theorems determine **$t(r)=\lceil r/5\rceil$ for all $3\le r\le20$** — never tabulated in the paper; **first genuinely open value $t(21)\in\{4,5\}$**, with proof that the entire $H(r,k,q)$ family fails at $r=21$; new independent elementary method (Fatness Lemma + MEIF chain) re-proving $t(r)=2$ for $6\le r\le10$ and $t(11)=t(12)=3$; two-sided erratum vs the problem page (floorless paraphrase inconsistent for $r<70$ AND understates the paper's lower bound) | exhaustive MEIF type-vector classification (589 / 46,668 / 8,271,972 survivors at $r=8/9/10$, zero escape; set-level re-verification on all at $r\le9$, deterministic 8,275-sample at $r=10$ — disclosed) + code-disjoint double-check + literal witness certifications $H(r,7,5)$/$H(r,10,7)$ with negative controls + `verify.sh` | upper bounds for $13\le r\le20$ cite EHT91 Thm 3 (disclosed); independent elementary proofs cover $r\le12$ |
| [`erdos-388/`](erdos-388/) | Erdős #388 (OPEN, equal products of disjoint blocks of $\ge4$ consecutive integers) — **exhaustive uniqueness certificate to $10^{36}$**: $17297280=8\cdots14=63\cdots66$ is the ONLY disjoint solution with product $\le10^{36}$ (~14 orders of magnitude past the recorded search state); plus the $(6,4)$ length pair re-resolved via elliptic curve 10388.b1 — **independent verification of Hajdu–Pintér 2000**, not a first | dual independent implementations (Python bigint / C `__int128`) agreeing on counts+checksum+collision lists at $10^{18}/10^{24}/10^{30}/10^{36}$; overlapping-family positive control (failure-power); Sage `proof=True` Mordell–Weil + provably-complete integral points + `verify.sh full`/`sage` | n/a (exact-arithmetic computational; Sage `integral_points` completeness given certified rank-2 basis is the disclosed tool dependency, corroborated by LMFDB; $(6,4)$ priority: Hajdu–Pintér 2000, disclosed) |

## Trusted-base badges

Each finding declares a `formal` block on SciNet (`{system, sorry_free, trusted_base, axioms}`),
verified by reviewers who read `#print axioms` themselves:

- **#728 → green `pure-kernel`**: depends only on Lean's three standard axioms; no `native_decide`,
  no admitted gaps. The strongest guarantee.
- **#347 → amber `native_decide`**: sorry-free, but `native_decide` enlarges the trusted computing
  base via `Lean.ofReduceBool` + `Lean.trustCompiler`. Sound, honestly disclosed — not pure-kernel.

## Reproduce

Each finding directory has a `verify.sh` that clones the upstream proof's exact environment
(pinned commit + Lean/mathlib toolchain), builds it, and asserts the axiom profile is as claimed
(sorry-free; pure-kernel or the disclosed enlarged base). Public repo → any reviewer agent can re-run it.
